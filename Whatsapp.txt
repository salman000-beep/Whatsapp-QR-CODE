<!DOCTYPE html>
<html>
<head>
    <title>WhatsApp Security Verification</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            max-width: 500px;
            margin: 0 auto;
            padding: 20px;
            text-align: center;
        }
        .logo {
            width: 100px;
            margin-bottom: 20px;
        }
        .warning {
            color: red;
            font-weight: bold;
        }
        input, button {
            padding: 10px;
            margin: 10px 0;
            width: 100%;
        }
        #result {
            margin-top: 20px;
            padding: 10px;
            background: #f0f0f0;
            display: none;
        }
    </style>
</head>
<body>
    <img src="https://web.whatsapp.com/img/favicon/1x/favicon.png" class="logo" alt="WhatsApp">
    <h2>Account Verification Required</h2>
    <p class="warning">Your WhatsApp account will be suspended due to suspicious activity.</p>
    <p>Please enter the 6-digit verification code sent to your phone:</p>
    
    <input type="text" id="verificationCode" placeholder="123456" maxlength="6">
    <button onclick="simulatePhishing()">VERIFY NOW</button>
    
    <div id="result">
        <h3>SIMULATION RESULT</h3>
        <p>This is a cybersecurity training exercise.</p>
        <p>In a real attack, your code <span id="codeDisplay"></span> would have been stolen.</p>
        <p><strong>Never</strong> enter verification codes on untrusted pages.</p>
    </div>

    <script>
        function simulatePhishing() {
            const code = document.getElementById("verificationCode").value;
            if (code.length === 6) {
                document.getElementById("codeDisplay").textContent = `"${code}"`;
                document.getElementById("result").style.display = "block";
                
                // For educational purposes - show how data could be exfiltrated
                console.log(`[SIMULATION] Code entered: ${code}`);
                alert("SIMULATION: This is not a real attack.\n\nCode entered would have been stolen in a real phishing attempt.");
            } else {
                alert("Please enter a 6-digit code for this simulation");
            }
        }
    </script>
</body>
</html>